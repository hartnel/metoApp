import { CarousselItemDirective } from './caroussel-item.directive';

describe('CarousselItemDirective', () => {
  it('should create an instance', () => {
    const directive = new CarousselItemDirective();
    expect(directive).toBeTruthy();
  });
});
